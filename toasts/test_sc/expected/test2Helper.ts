// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v4.23.2

import { Writer, Reader, Protobuf } from "as-proto/assembly";

export class test2Helper {
  static encode(message: test2Helper, writer: Writer): void {
    writer.uint32(10);
    writer.bytes(message.arg);
  }

  static decode(reader: Reader, length: i32): test2Helper {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new test2Helper();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.arg = reader.bytes();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  arg: Uint8Array;

  constructor(arg: Uint8Array = new Uint8Array(0)) {
    this.arg = arg;
  }
}

export function encodetest2Helper(message: test2Helper): Uint8Array {
  return Protobuf.encode(message, test2Helper.encode);
}

export function decodetest2Helper(buffer: Uint8Array): test2Helper {
  return Protobuf.decode<test2Helper>(buffer, test2Helper.decode);
}
